import React from 'react';
// DEPRECATED: This component has been replaced by ProjectSettingsModal.tsx
// Keeping file temporarily to prevent build breaks if referenced elsewhere, 
// but it renders nothing.

const ProjectSettingsSidebar: React.FC<any> = () => {
  return null; 
};

export default ProjectSettingsSidebar;